strategy:
  fail-fast: false

  matrix:
    os: [ubuntu-latest, windows-latest]

    python-version:
      - "3.10"
      - "3.11"
      - "3.12"

    exclude:
      - os: windows-latest
        python-version: "3.10"
